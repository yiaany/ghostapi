interface:
  display_name: "Ghostapi"
  short_description: "Repo-specific patterns and workflows for ghostapi"
  default_prompt: "Use the ghostapi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
